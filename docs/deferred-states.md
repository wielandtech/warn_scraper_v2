# Deferred state scrapers

States from the V1 active set whose live sources have drifted in ways that
need investigation (URL moved, new bot protection, page rebuilt with
JS-rendered content). Listed here so the Phase-4-style enrichment / heal
agent has a queue to work through later.

Last checked: 2026-05-27.

| State | V1 URL | What changed | Next step |
|-------|--------|--------------|-----------|
| **CT** | `ctdol.state.ct.us/progsupt/bussrvce/warnreports/warn{year}.htm` | **Resolved** — site migrated to `dolpublicdocumentlibrary.ct.gov`. Scraper hits `GetPagedBlobs` JSON API (Azure Blob Storage). Parses employer/city/date from PDF filenames; falls back to `modifiedDate`. Implemented in `warn_v2/scrapers/states/ct.py`. | — |
| **GA** | `https://www.dol.state.ga.us/public/es/warn/searchwarns` | **Resolved** — Playwright scraper fetches `tcsg.edu/warn-public-view/`. Implemented in `warn_v2/scrapers/states/ga.py` (commit `a537a15`). | — |
| **MI** | `https://milmi.org/warn` | **Resolved** — Sitecore XA search API (`/sxa/search/results/`) returns JSON with HTML card fragments directly; no Playwright needed. Implemented in `warn_v2/scrapers/states/mi.py` (commit `683800b`). | — |
| **NJ JSP** | `lwd.state.nj.us/WorkForceDirectory/warn.jsp?...` | Behind Incapsula (403 + JS challenge). V2 falls back to the still-live yearly PDF at `nj.gov/labor/assets/PDFs/WARN/{year}_WARN_Notice_Archive.pdf` (see `warn_v2/scrapers/states/nj.py`). | If the PDF ever stops being published, scraping the JSP page would need a real browser (Playwright) — not worth it. |
| **OH** | `jfs.ohio.gov/warn/current.stm` | **Resolved** — JSON blob embedded in hidden `<div>` on the public notices page; httpx + BeautifulSoup. Implemented in `warn_v2/scrapers/states/oh.py` (commit `683800b`). | — |
| **PA** | `dli.pa.gov/Individuals/Workforce-Development/warn/notices/Pages/{Month}-{Year}.aspx` | **Resolved** — new URL confirmed live at `pa.gov/agencies/dli/programs-services/workforce-development-home/warn-requirements/warn-notices`. AEM accordion page, plain HTML, no JS required. 300 accordion items covering 2023-present. Implemented in `warn_v2/scrapers/states/pa.py`. | — |
| **OR** | `ccwd.hecc.oregon.gov/Layoff/WARN` | **Resolved** — county filter (`?County=Name&page=N`) restricts to OR only. Scraper iterates all 36 counties with pagination. Implemented in `warn_v2/scrapers/states/or_.py`. | — |
| **IA** | `iowawf.gov/147/WARN-Notices` | **Resolved** — page shows JS Tableau visualization, but has a stable ADA-compliant Excel download at `workforce.iowa.gov/media/3025/download?inline`. Scraper fetches cumulative XLSX directly. Implemented in `warn_v2/scrapers/states/ia.py`. | — |
| **MA** | `mass.gov/lists/...warn-act-layoff-list` | **Resolved** — Playwright discovers CSV links on the index page; httpx downloads the weekly CSVs directly. Implemented in `warn_v2/scrapers/states/ma.py` (commit `683800b`). | — |
| **MN** | `mn.gov/deed/business/downsizing-restructuring/warn/` | **Resolved** — Wayback Machine CDX API discovers PDF URLs without bot protection; pdfplumber parses rows where WARN Act = YES. Implemented in `warn_v2/scrapers/states/mn.py` (commit `683800b`). | — |
| **MO** | `jobs.mo.gov/warn` | **Resolved** — Playwright iterates per-year pages at `jobs.mo.gov/warn/YYYY` (2019–current); BeautifulSoup parses each HTML table. Implemented in `warn_v2/scrapers/states/mo.py` (commit `683800b`). | — |
| **WV** | *(no V1 scraper)* | **Resolved** — Playwright bypasses Cloudflare on `workforcewv.org/job-seeker/layoffs-downsizing/warn-listing/`; employer name and notice date extracted from PDF anchor text. Implemented in `warn_v2/scrapers/states/wv.py` (commit `e2a3a28`). | — |
| **TN** | `tn.gov/workforce/.../reports.html` | Scraper fully implemented and tested in `warn_v2/scrapers/states/tn.py`. `register()` commented out because `tn.gov` resets TLS connections from container/server IPs. Works fine locally. | Needs proxy or residential IP for container deployment. Re-enable by uncommenting `register(TNScraper())`. |
| **OK** | `okjobmatch.com/search/warn_lookups?...` | Migrated to **Salesforce Experience Cloud** at `employoklahoma.gov/Participants/s/warnnotices`. Aura endpoint requires a csrfV2 `aura.token` generated by page-load JS; all data actions return empty without it. Stub scraper in `warn_v2/scrapers/states/ok.py`. | Requires Playwright: load page, capture `aura.token`, intercept the Aura action returning WARN records. |
| **AR** | *(no V1 scraper)* | WARN notices are confidential under A.C.A. § 11-10-314; no public disclosure required or published. Stub scraper in `warn_v2/scrapers/states/ar.py`. | No action — state law prohibits public release. |
| **NH** | *(no V1 scraper)* | NH does not publish WARN notices online; `nhes.nh.gov` blocks server IPs (Akamai). Notices only via public records request to `masslayoff@nhes.nh.gov`. Stub scraper in `warn_v2/scrapers/states/nh.py`. | No action — no public data source. |
| **WY** | *(no V1 scraper)* | WY DWS is Cloudflare-protected and only links to an informational PDF; no structured WARN listing exists. Stub scraper in `warn_v2/scrapers/states/wy.py`. | No action — no public structured source. |

Stubs (AR, NH, WY) raise `ScrapeFailed` in `fetch()` with a clear message; the
runner logs `fetch_failed` cleanly. TN is fully functional locally. OK has a stub
pending a Playwright solution for the Salesforce/Aura token flow.
